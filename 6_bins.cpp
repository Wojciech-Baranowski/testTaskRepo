#include<bits/stdc++.h>
using namespace std; 
int t, n, m, k; 

int binSearch() {
	if (m * t < k) {
		return 0; 
	}
	int a = 0, b = m; 
	else {
		if (a == b) {
			if (a = min) {
				return a; 
			}
			else {
				return -1; 
			}
		 }
		 int srodek = (a + b)>>1; 
		 if ()
	}
}
int main() {
	cin >> t >> n >> m >> k;	//>>1 przesunięcie bitowe 
	cout << binSearch() << "\n"; 
	return 0; 
}
